module.exports = {
  reactStrictMode: true,

  images: {
    domains: ['i.picsum.photos', 'image.tmdb.org']
  },

  experimental: {
    // ssr and displayName are configured by default
    styledComponents: true
  }
}
